# 🚀 部署到 GitHub Pages 完整指南

## 第一步：在 GitHub 创建仓库

1. 打开浏览器，访问 **https://github.com**
2. 登录你的 GitHub 账号（如果没有账号，先注册一个）
3. 点击右上角的 **+** 号，选择 **New repository**
4. 填写仓库信息：
   ```
   Repository name: lab-website
   Description: Mount Huaguo Lab Website
   选择: Public (公开)
   ❌ 不要勾选任何选项（README、.gitignore等）
   ```
5. 点击绿色按钮 **Create repository**
6. **记住仓库地址**，格式类似：`https://github.com/你的用户名/lab-website.git`

---

## 第二步：推送代码到 GitHub

打开终端（Terminal），复制粘贴以下命令：

```bash
# 1. 进入项目目录
cd /Users/javen/Documents/项目/实验室主页

# 2. 查看当前分支
git branch

# 3. 如果是 master，改名为 main（GitHub 推荐）
git branch -M main

# 4. 添加远程仓库（替换下面的 URL 为你的仓库地址！）
git remote add origin https://github.com/你的用户名/lab-website.git

# 5. 推送代码
git push -u origin main
```

**注意：** 第4步要替换成你自己的仓库地址！

---

## 第三步：启用 GitHub Pages

1. 在 GitHub 仓库页面，点击顶部的 **Settings**（设置）
2. 在左侧菜单找到 **Pages**
3. 在 **Source** 下拉菜单中选择：**GitHub Actions**
4. 等待 1-2 分钟，GitHub 会自动构建网站

---

## 第四步：访问你的网站

网址格式：
```
https://你的用户名.github.io/lab-website/
```

例如，如果你的 GitHub 用户名是 `zhangsan`，仓库名是 `lab-website`，那么网址就是：
```
https://zhangsan.github.io/lab-website/
```

**首次部署需要等待 2-5 分钟**，之后每次更新只需要 1-2 分钟。

---

## 📝 如何更新网站内容？

### 方法1：GitHub 网页直接编辑（推荐给团队成员）

1. 打开 GitHub 仓库
2. 找到要编辑的文件，比如 `research/index.md`
3. 点击文件，然后点击右上角的 ✏️（铅笔图标）
4. 编辑内容
5. 滚动到页面底部，填写 Commit message（如："添加新论文"）
6. 点击 **Commit changes**
7. 等待 1-2 分钟，网站自动更新！

### 方法2：本地编辑（适合你自己）

```bash
# 1. 进入项目目录
cd /Users/javen/Documents/项目/实验室主页

# 2. 编辑文件（用任何编辑器）
# 例如编辑论文列表
open research/index.md

# 3. 保存后，提交更改
git add .
git commit -m "更新内容"
git push

# 4. 等待 1-2 分钟，网站自动更新
```

---

## 🎓 如何让团队成员参与编辑？

### 选项1：添加协作者（简单）

1. 在 GitHub 仓库页面，点击 **Settings**
2. 点击左侧的 **Collaborators**
3. 点击 **Add people**
4. 输入团队成员的 GitHub 用户名或邮箱
5. 他们会收到邮件邀请，接受后就可以直接编辑了

### 选项2：使用 Pull Request（规范）

1. 团队成员 Fork 你的仓库
2. 在自己的 Fork 中编辑
3. 提交 Pull Request
4. 你审核后合并

---

## ⚠️ 常见问题

### Q1: 推送代码时要求输入用户名和密码？

GitHub 已经不支持密码登录了，需要使用 Personal Access Token：

1. 打开 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 **Generate new token (classic)**
3. 勾选 `repo` 权限
4. 生成后复制 token（只显示一次！）
5. 推送时，用户名填你的 GitHub 用户名，密码填这个 token

### Q2: 网站打开后样式不对？

检查 `_config.yaml` 中的 `baseurl` 设置：
```yaml
baseurl: "/lab-website"  # 填你的仓库名
```

### Q3: 如何绑定自定义域名？

1. 购买域名（如 `lab.zjut.edu.cn`）
2. 在域名提供商处添加 CNAME 记录，指向 `你的用户名.github.io`
3. 在 GitHub Pages 设置中填写自定义域名
4. 等待 DNS 生效（可能需要几小时）

---

## 🔧 高级配置

### 启用 HTTPS（免费SSL证书）

GitHub Pages 自动提供 HTTPS，在 Settings → Pages 中勾选 **Enforce HTTPS** 即可。

### 查看构建状态

1. 在仓库页面点击 **Actions**
2. 可以看到每次推送的构建日志
3. 如果构建失败，点击查看错误信息

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看 GitHub Pages 官方文档
2. 查看仓库的 Actions 日志
3. 或者联系我

---

**祝你部署顺利！🎉**

