# 实验室主页使用说明

## 📋 项目概览

你的实验室主页已经成功初始化！这个网站基于 **Greenelab Lab Website Template**，是一个专为科研实验室设计的现代化网站模板。

### ✨ 核心特性

- ✅ 自动从 Google Scholar、PubMed、ORCID 导入论文
- ✅ 团队成员管理系统（含照片、角色、社交链接）
- ✅ 研究项目展示
- ✅ 博客/新闻发布系统
- ✅ 完全基于 Markdown 文件管理，无需数据库
- ✅ 自动化 CI/CD，推送即部署
- ✅ 移动端友好的响应式设计

---

## 🚀 快速开始

### 1. 安装依赖

本项目基于 Jekyll（Ruby），需要先安装相关依赖：

```bash
# 进入项目目录
cd /Users/javen/Documents/项目/实验室主页

# 安装 Ruby gems
bundle install

# 安装 Python 依赖（用于自动引用论文）
pip install -r _cite/requirements.txt
```

### 2. 本地预览

```bash
# 启动本地开发服务器
bundle exec jekyll serve

# 浏览器访问：http://localhost:4000
```

### 3. 自动更新论文引用

```bash
# 运行引用更新脚本
python _cite/cite.py
```

---

## 📝 配置你的实验室信息

### 第一步：修改基本配置

编辑 `_config.yaml` 文件，更新以下信息：

```yaml
# 网站基本信息
title: 你的实验室名称
subtitle: 所属机构
description: 实验室简介描述

# 社交媒体和联系方式
links:
  email: your-lab@university.edu
  orcid: 你的ORCID ID
  google-scholar: 你的Google Scholar ID
  github: 你的GitHub用户名
  twitter: 推特账号（可选）
```

### 第二步：更新首页内容

编辑 `index.md`，修改首页的介绍文字和亮点展示。

### 第三步：添加团队成员

在 `_members/` 目录下创建成员文件，格式如下：

```markdown
---
name: 张三
image: images/members/zhangsan.jpg
role: pi  # 角色：pi（负责人）、phd（博士生）、masters（硕士生）、undergrad（本科生）
affiliation: XX大学
links:
  email: zhangsan@university.edu
  orcid: 0000-0001-xxxx-xxxx
  google-scholar: xxxxx
---

个人简介...
```

### 第四步：导入论文列表

有两种方式导入论文：

#### 方式 1：自动导入（推荐）

编辑 `_data/sources.yaml`，添加论文的 DOI 或其他标识符：

```yaml
- id: doi:10.xxxx/xxxxx
  type: paper
  description: 论文的简短描述
  image: 论文配图URL（可选）
  tags:
    - 机器学习
    - 计算机视觉
```

然后运行：
```bash
python _cite/cite.py
```

#### 方式 2：从 ORCID 自动同步

在 `_config.yaml` 中配置你的 ORCID ID，系统会自动同步你的论文列表。

### 第五步：添加研究方向

编辑 `research/index.md`，描述你的研究方向和主要成果。

### 第六步：上传实验室 Logo 和图片

- Logo：替换 `images/logo.svg`
- 背景图：替换 `images/background.jpg`
- 成员照片：上传到 `images/members/` 目录

---

## 🌐 部署到 GitHub Pages

### 1. 创建 GitHub 仓库

在 GitHub 上创建一个新仓库，例如：`your-lab/lab-website`

### 2. 推送代码

```bash
git remote add origin https://github.com/your-lab/lab-website.git
git branch -M main
git push -u origin main
```

### 3. 启用 GitHub Pages

1. 进入仓库的 **Settings** → **Pages**
2. Source 选择：**GitHub Actions**
3. 等待自动部署完成（约 1-2 分钟）
4. 访问：`https://your-lab.github.io/lab-website/`

### 4. 绑定自定义域名（可选）

在 GitHub Pages 设置中可以绑定自己的域名，如：`lab.university.edu`

---

## 📂 项目结构说明

```
实验室主页/
├── _config.yaml          # 网站主配置文件
├── index.md              # 首页内容
├── _members/             # 团队成员信息
│   ├── member1.md
│   └── member2.md
├── _posts/               # 博客/新闻文章
│   └── 2025-01-15-news.md
├── _data/
│   ├── citations.yaml    # 自动生成的论文引用数据
│   ├── sources.yaml      # 论文源数据（需手动编辑）
│   └── projects.yaml     # 项目数据
├── research/             # 研究方向页面
│   └── index.md
├── team/                 # 团队页面
│   └── index.md
├── blog/                 # 博客页面
│   └── index.md
├── contact/              # 联系方式页面
│   └── index.md
├── images/               # 图片资源
│   ├── logo.svg
│   ├── background.jpg
│   └── members/
└── _cite/                # 自动引用脚本
    └── cite.py
```

---

## 🎨 自定义样式

如需修改网站样式，编辑 `_styles/` 目录下的 SCSS 文件。

主题颜色配置在 `_styles/-theme.scss`。

---

## 📚 更多文档

- [官方文档](https://greene-lab.gitbook.io/lab-website-template-docs)
- [GitHub 仓库](https://github.com/greenelab/lab-website-template)

---

## ❓ 下一步需要你提供的信息

为了完成网站的配置，请准备以下信息：

### 1. 实验室基本信息
- 实验室全称（中英文）
- 所属机构/大学
- 研究方向简介（2-3 句话）
- Logo 图片（如有）

### 2. 论文列表
可以提供以下任一方式：
- Google Scholar 个人主页链接
- ORCID ID
- 论文 DOI 列表
- BibTeX 文件

### 3. 团队成员
对于每位成员，提供：
- 姓名
- 角色（负责人/博士生/硕士生/本科生等）
- 简介（2-3 句话）
- 照片（可后续补充）
- 联系邮箱
- ORCID/Google Scholar（如有）

### 4. 研究方向
- 主要研究方向描述
- 代表性项目介绍
- 相关图片或可视化（如有）

### 5. 联系方式
- 实验室地址
- 联系邮箱
- 实验室电话（可选）

---

**提供上述信息后，我将帮你完成所有配置！** 🎉

