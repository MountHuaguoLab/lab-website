# ============================================================
# 网站基本配置
# ============================================================
title: 实验室主页
subtitle: 研究团队
description: 专注于前沿科研，致力于创新突破
header: images/background.jpg
footer: images/background.jpg
proofer: false

# ============================================================
# 社交媒体和学术链接
# ============================================================
links:
  email: contact@lab.edu
  # Jianwei Zheng 的 Google Scholar
  google-scholar: X0wntOEAAAAJ
  github: your-lab
  # twitter: YourLabHandle
  # youtube: YourLabChannel

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
